# Base ----------------------------------------
matplotlib==3.10.9
numpy==1.26.4
opencv-python==4.11.0.86
opencv-python-headless==4.10.0.84
pillow==12.2.0
pillow-avif-plugin==1.5.5
PyYAML==6.0.1
requests==2.34.2
requests-toolbelt==1.0.0
scipy==1.17.1
tqdm==4.67.3

# detection -----------------------------------
# Inference runs through ONNX Runtime (see objectdetectorONNX.py). yolov5/torch
# were only used offline to export the .onnx model, so they're intentionally NOT
# installed here — torch pulls a multi-GB CUDA wheel stack that nothing imports
# and that overflowed the image build. For GPU inference later (e.g. on the AGX),
# swap onnxruntime -> the Jetson onnxruntime-gpu build; torch stays out.
onnxruntime==1.26.0

# data / clustering ---------------------------
pandas==3.0.3
hdbscan==0.8.43
